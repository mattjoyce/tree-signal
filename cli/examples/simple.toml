# Simple Tree Signal CLI configuration
# Forward all logs to a single channel

[api]
url = "http://localhost:8013"

[defaults]
channel = "logs"
severity = "info"

[decay]
hold_seconds = 30.0
decay_seconds = 10.0

[performance]
batch_size = 10
rate_limit = 100
