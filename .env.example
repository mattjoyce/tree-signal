# Tree Signal API Configuration
# Copy this file to .env and configure for your setup

# Tree Signal API URL (default: http://192.168.20.4:8013)
TREE_SIGNAL_URL=http://192.168.20.4:8013

# Optional API key for authenticated access
TREE_SIGNAL_API_KEY=

# Color Assignment Mode
# - increment: Sequential colors (first channel=hue 0°, second=101°, etc.) [DEFAULT]
# - hash: Deterministic hash-based (same channel always same color)
COLOR_ASSIGNMENT_MODE=increment

# Color Inheritance Mode
# - unique: Every channel gets unique color (maximum distinction) [DEFAULT]
# - root: Children inherit root's hue with +5° variations (color families)
# - family: Children inherit exact hue, vary lightness by depth (strong grouping)
COLOR_INHERITANCE_MODE=unique
